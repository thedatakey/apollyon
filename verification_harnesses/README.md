# Verification harnesses

Each harness must state the property, input assumptions, unwinding/loop bounds,
tool version, command, and result. A passing bounded-model-checking run only
supports the documented bounded claim; it is not a proof of arbitrary-program security.
