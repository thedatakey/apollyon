# Apollyon

After the v0.4.0 packages are published:

```sh
npx apollyon scan .
```

Native optional packages supply the binary. Installation does not run a download
script or compile scanned projects. Supported: Linux x64, macOS arm64/x64, Windows
x64. Findings are review candidates; inspect completeness and coverage.
