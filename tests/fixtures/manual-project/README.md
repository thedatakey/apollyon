# Mixed manual-project fixture

This deliberately vulnerable, non-buildable source tree exercises Apollyon's
cross-language discovery, stable rule IDs, default dependency exclusions, and
custom `generated` exclusion. It is test data, not production example code.
